name: Deploy Roadmap AI

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy application
        run: |
          cd ~/roadmap.ai
          git pull origin main
          sudo docker compose up -d --build
